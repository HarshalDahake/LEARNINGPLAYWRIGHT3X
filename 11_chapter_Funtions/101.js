//console.log(MAX_RETRIES); // TDZ : Cannot access before initialization.
//const MAX_RETRIES = 3;
//MAX_RETRIES = 4; // Re-Assignment to const is not possible.

const score = 100
// score = score+10; // Re-Assignment to const is not possible.

const pi = 3.14;
const prod_api_url = "https://app.vwo.com/#login";
//prod_api_url = "https://google.com"; // Re-Assignment to const is not possible.

const qa_api_url = "https://qa.vwo.com/#login";

let abc = "anil";
console.log(abc); // it shows diff bet const and let.